<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teamproject.usedmarket.repository.mybatis.MessageMapper">

    <resultMap id="messageResultMap" type="teamproject.usedmarket.domain.chat.ChatMessage">
        <id property="id" column="id" />
        <result property="content" column="content" />
        <result property="sender" column="sender" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="getAllMessages" resultMap="messageResultMap">
        SELECT * FROM chat_message ORDER BY created_at ASC
    </select>

    <insert id="insertMessage" parameterType="teamproject.usedmarket.domain.chat.ChatMessage">
        INSERT INTO chat_message (content, sender) VALUES (#{content}, #{sender})
    </insert>


</mapper>